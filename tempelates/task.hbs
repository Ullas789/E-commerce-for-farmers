<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delivery Info</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

 /* Create a container for the blurred background */
        .background-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            filter: blur(7px); /* Apply background blur effect */
        }

        /* Style the background image */
        .background-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        }

        .delivery-info {
            text-align: center;
            margin-bottom: 20px;
        }

        .task {
            text-align: left;
            margin-bottom: 20px;
        }

        .task p {
            margin: 5px 0;
        }

        .back-button {
            text-align: center;
            margin-top: 20px;
        }

        .back-button button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s ease-in-out;
        }

        .back-button button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="background-container">
        <img class="background-image" src="bg1.jpg" alt="Background Image">
    </div>
    <div class="container">
        <h1>Delivery Info</h1>
        <div class="delivery-info">
            <p><b>Your tasks are:</b></p>
        </div>
        <div class="task">
            <p>Delivery 1: Item 1</p>
            <p>Address 1</p>
        </div>
        <div class="task">
            <p>Delivery 2: Item 2</p>
            <p>Address 2</p>
        </div>
        <div class="task">
            <p>Delivery 3: Item 3</p>
            <p>Address 3</p>
        </div>
        <!-- Add more deliveries here as needed -->
        <form action="/home">
        <div class="back-button">
            <button>Back</button>
        </div>
        </form>
    </div>
</body>
</html>
